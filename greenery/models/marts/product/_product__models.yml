version: 2

models:
  - name: page_views
    description: website page views. contains only events with the event_type = 'page view'

  - name: daily_products_sold
    description: a rollup of the number of products sold, per day 

  - name: daily_delivered_order_totals 
    description: order totals, by day for delivered orders
  
  - name: daily_orders_by_status 
    description: the number of orders per day, per status 